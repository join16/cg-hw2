#! /bin/sh

DEST=hw2_2013147544_조인

rm -rf ${DEST}
mkdir ${DEST}

cp -aR `pwd`/bin `pwd`/${DEST}
cp -aR `pwd`/src `pwd`/${DEST}

cp CMakeLists.txt ${DEST}
cp Makefile ${DEST}
cp README.md ${DEST}
cp capture.jpg ${DEST}

zip -r ${DEST}.zip ${DEST}/*